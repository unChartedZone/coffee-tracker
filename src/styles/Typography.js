import { createGlobalStyle } from 'styled-components';

const Typograhpy = createGlobalStyle`
  .title {
    font-size: 3rem;
  }

  .text {
    font-size: 1.6rem;
  }

  .text-center {
      text-align: center;
  }
`;

export default Typograhpy;
